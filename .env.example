VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Analytics. GA4 already has a live default baked into src/lib/analytics.ts (G-PZSEQGE53P) —
# only set VITE_GA_MEASUREMENT_ID here if you need to override it. Meta Pixel is off until
# VITE_META_PIXEL_ID is set below, then redeploy.
VITE_GA_MEASUREMENT_ID=
VITE_META_PIXEL_ID=
