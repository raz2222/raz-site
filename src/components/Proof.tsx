import { Reveal } from "./Reveal"

export function Proof() {
  return (
    <section className="py-20 border-t border-white/10">
      <div className="container">
        <Reveal className="font-mono text-xs uppercase tracking-wide text-dim text-center">
          More projects and numbers land here as the portfolio grows — real data only.
        </Reveal>
      </div>
    </section>
  )
}
